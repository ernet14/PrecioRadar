import { createStubProvider } from "@/providers/stores/stubProvider";

export const cetrogarProvider = createStubProvider({
  name: "cetrogar",
  todo: "Use an official or permitted integration before enabling real Cetrogar data. Do not scrape aggressively or invent APIs.",
});
