import { createStubProvider } from "@/providers/stores/stubProvider";

export const tiendamiaProvider = createStubProvider({
  name: "tiendamia",
  todo: "Future provider. Keep inactive until an official or permitted TiendaMia integration is defined.",
});
