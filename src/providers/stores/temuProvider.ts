import { createStubProvider } from "@/providers/stores/stubProvider";

export const temuProvider = createStubProvider({
  name: "temu",
  todo: "Future provider. Keep inactive until an official or permitted Temu integration is defined.",
});
