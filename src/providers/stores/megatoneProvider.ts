import { createStubProvider } from "@/providers/stores/stubProvider";

export const megatoneProvider = createStubProvider({
  name: "megatone",
  todo: "Use an official or permitted integration before enabling real Megatone data. Do not scrape aggressively or invent APIs.",
});
