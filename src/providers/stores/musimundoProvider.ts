import { createStubProvider } from "@/providers/stores/stubProvider";

export const musimundoProvider = createStubProvider({
  name: "musimundo",
  todo: "Use an official or permitted integration before enabling real Musimundo data. Do not scrape aggressively or invent APIs.",
});
