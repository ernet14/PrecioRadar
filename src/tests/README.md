# Tests

No test runner is configured yet.

When the project adds a test setup, the first coverage should target pure helper
logic such as:

- `formatCurrencyARS`
- `formatDate`
- `slugify`
- `detectInputType`
- `isMercadoLibreUrl`
- `normalizeProductName`
